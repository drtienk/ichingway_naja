# 易經股市應證 Web App — 專案唯一真實來源

> 本檔是本專案唯一的架構與接手說明（Single Source of Truth）。AI 或開發者開始工作前必須先讀本檔；架構、規則或檔案責任改變時，必須同步更新本檔。不要另外建立內容重複的 README、進度檔或規格檔。

## 專案目標

這是一個可直接開啟 HTML 使用的 I-ChingWay Web App，包含四個彼此獨立、可互相導覽的功能：

1. **易經股市應證**：既有功能，將財爻狀態與 Yahoo Finance 股價資料對照。
2. **京房納甲裝卦**：獨立功能，只負責排出完整納甲卦盤，不抓股價。
3. **萬年曆**：國曆／農曆月曆、日期詳情、年/月/日干支與旬空，支援 1901–2100 年。
4. **風水羅盤**：地址、Google Maps 連結、座標或裝置定位下盤；支援衛星圖、正北／磁北、二十四山、拖曳與量向線。

兩個功能可以互相導覽，但不得把裝卦核心依賴股票 API，也不得為了修改裝卦頁而改壞既有股市頁。

## 使用方式

- 直接雙擊根目錄 `index.html`；它會以「京房納甲裝卦」作為預設功能。
- 裝卦頁上方依序提供「裝卦、股市應證、萬年曆、風水羅盤」四個按鈕。
- 需要直接開啟既有股市功能時，可雙擊 `易經股市應證.html`。
- 兩頁都必須支援 `file://` 離線開啟；不可引入需要本機伺服器才能載入的 ES modules。
- 股市頁抓取行情時需要網路；納甲頁完全離線。

## OneDrive 工作限制

本專案位於 OneDrive 同步目錄。所有工作必須以「少量、穩定、正式檔案」為原則，避免大量同步與版本衝突。

1. **禁止在專案內產生暫存與快取**：不得建立 `.cache`、`.tmp`、`temp`、測試截圖、瀏覽器快照、coverage 或大量中間檔。
2. **禁止在專案內安裝依賴**：不得建立 `node_modules`、Python 虛擬環境或套件快取。本專案維持零安裝、直接開啟。
3. **禁止建立建置輸出**：不得新增 `dist`、`build`、`.next` 等可重新產生的資料夾。
4. 需要暫存檔時，使用作業系統的暫存目錄，完成後清除；不要放進本專案或其子目錄。
5. 測試優先使用記憶體或直接執行現有檔案，不複製整個專案，不產生測試報告檔。
6. 新增永久檔案前先確認它有獨立責任；能更新既有正式檔案時，不建立日期版、備份版或副本。
7. 手機版面至少驗證 iPhone 13 mini（375×812）、iPhone 8（375×667）與 iPad mini（768×1024）直向 viewport；不得出現頁面水平溢出、操作元件超出畫面或主要按鈕無法觸及。
8. 手機裝卦首屏採「立即操作」配置：頁首只顯示 `I-ChingWay`，語言與四個功能入口保持緊湊；中文預設模式下，iPhone 8（375×667）開啟後必須完整看到上卦、下卦與排卦按鈕，不需先捲動。占事、動爻、日期及顯示設定排在主要選卦操作之後。

## 檔案責任

```text
.
├─ AGENTS.md                 # 本檔；唯一真實來源
├─ index.html                # 預設入口；轉入獨立納甲頁
├─ 易經股市應證.html          # 既有股市功能；維持單檔，僅做必要維護
├─ .github/workflows/pages.yml # GitHub Pages 靜態網站自動部署
├─ najia/
│  ├─ index.html             # 獨立納甲頁的語意結構
│  ├─ icon-najia-180.png     # iPhone 主畫面與瀏覽器使用的納甲圖示
│  ├─ calendar.html          # 萬年曆完整獨立頁
│  ├─ fengshui-compass.html  # 風水羅盤完整獨立頁
│  ├─ styles.css             # 獨立納甲頁樣式
│  ├─ core.js                # 純規則與資料；不操作 DOM，可供 Node 測試
│  └─ app.js                 # 納甲頁 UI、事件與渲染
└─ tests/
   └─ najia-core.test.js     # 64 卦與動爻規則測試
```

## 京房納甲規則基準

- 爻序：陣列索引 0–5 對應初爻至上爻。
- 上下卦資料、八宮卦序、世應位置集中在 `najia/core.js`。
- 本卦六親以本卦所屬宮五行為基準。
- **化爻六親仍以本卦宮五行為基準**，不可改用變卦所屬宮五行。
- 變卦的納干、納支、陰陽取自變後上下卦。
- 卦中缺少的六親，依本宮純卦對應爻位列為伏神；卦盤必須實際顯示。
- 起卦輸入使用裝置本地日期時間；顯示與干支計算一律轉為中原標準時間（UTC+8）。
- 年柱以立春切年、月柱以十二節近似日期切月、日柱以中原時間計算；中原時間 23:00 起進入次日日柱。
- 結果必須同時顯示年、月、日天干地支，不可只顯示日柱。
- 六獸依日干，自初爻往上排列。
- 每爻的固定閱讀順序為：左側「六親＋納甲（伏神附於左側）」、中央「爻線＋世應」、化爻資訊、最右側「六獸」。六親與納甲必須在同一水平列；桌面表格、手機逐爻版與匯出 PNG 必須一致。
- 有動爻時，手機結果、桌面表格與匯出 PNG 均採「左變卦、右本卦」排列；變卦只在實際動爻的位置顯示變爻爻線、六親與納甲，其餘變卦爻位留空。本卦仍完整顯示六爻，六獸固定在最右側。
- 動爻符號必須疊在爻線中央：老陽以紅色圓圈標示，老陰以紅色叉號標示，不可排在爻線旁邊。
- 「世／應」標示必須置於對應爻線正下方，不可與爻線或六親、六獸並排。
- 顯示語言支援「中文、English、中英」三種模式；語言只影響顯示文字，不改變納甲計算資料，選擇保存在瀏覽器本機。
- 頁首品牌固定顯示為 `I-ChingWay`。八卦選擇按鈕預設只顯示「天、澤、火、雷、風、水、山、地」八個自然名稱，不重複顯示乾兌離震等卦名；使用者可用「顯示卦象」選項開關 ☰、☱ 等符號，選擇保存在瀏覽器本機。
- 八卦自然名稱是選擇格的主要視覺。介面提供 `A`／`A+` 卦名字體按鈕，預設 `A+`；中文大字約 52px（比原 32px 大約 62%），English 與中英模式在窄螢幕自動改為兩欄以容納放大字。大小選擇保存在瀏覽器本機，所有模式均不可溢出、截斷或撐破按鈕。
- 占事輸入欄置於「設定卦象」上方。八卦選擇另提供 `123` 數字模式，固定映射為 1天、2澤、3火、4雷、5風、6水、7山、8地；模式保存在瀏覽器本機。
- 手機裝卦區最上方固定先顯示「顯示卦象、A／A+、123」控制列，再顯示占事與卦象選擇。四個功能入口的中文採接近滿格的大字，需在單列四格內盡量填滿但不得溢出；英文／中英模式可自動縮小或改兩欄。
- iPhone 加入主畫面時使用 `najia/icon-najia-180.png`：深藍底、金色六爻與干支運行軌道意象，不使用中文字；圖示保持單一最佳化正式檔，避免 OneDrive 同步大型生成原圖或多份尺寸副本。
- 本專案顯示用語依使用者要求，將傳統「螣蛇」顯示為「呈蛇」。
- 旬空依起卦日干支計算。
- 日期用於年、月、日干支、六獸與旬空；目前不顯示時柱，月柱採十二節的固定近似交節日。

## 萬年曆與風水羅盤基準

- 兩頁功能移植自參考專案 `4. 六爻裝卦app`，保留原本完整能力，不把其程式複製回納甲 `app.js`。
- 萬年曆使用瀏覽器 `Intl` Chinese calendar，無外部套件；支援國曆／農曆、1901–2100、日期跳轉、五級字體、中文／English／中英、年/月/日干支與旬空。
- 萬年曆與裝卦共用 `najia_display_language` 語言偏好；同時相容參考版舊鍵 `liuyao_lang`。萬年曆檢視與字級仍使用 `liuyao_calendar_view`、`liuyao_calendar_font_size`。
- 風水羅盤必須保留：地址與 Google Maps 連結解析、經緯度輸入、目前位置、搜尋紀錄、街道／衛星圖、正北／磁北、WMM 磁偏角、二十四山、大小、透明度、羅盤顯示切換、拖曳、量向線與控制列收合。
- 羅盤需要網路載入 Leaflet、geomagnetism、OpenStreetMap／Esri 圖磚及 Nominatim 地址服務；定位需要 HTTPS 與使用者授權。納甲與萬年曆仍可完全離線。

## 修改原則

1. 先改 `najia/core.js` 的規則，再改 UI；不要在 `app.js` 複製另一份規則。
2. 規則修改後必須執行 `node tests/najia-core.test.js`。
3. 新增卦盤欄位時，桌面與手機都要可讀；表格外層必須保留橫向捲動。
4. 不使用外部 CDN、字型或 API 來完成納甲頁。
5. 不把歷史紀錄或使用者輸入寫進資料庫；若日後增加保存功能，預設只用本機儲存並在本檔說明。
6. 現有 `易經股市應證.html` 尚有舊版裝卦邏輯，供股市功能使用；新的正確規則以 `najia/core.js` 為準。未經明確任務，不重構或替換舊頁。

## 已知邊界

- 納甲頁目前支援：上下卦、零至六個動爻、本卦、變卦、八宮、宮五行、世應、納干支、五行、六親、伏神、六獸、旬空。
- 裝卦前可填寫占事與本地日期時間；畫面與匯出 PNG 均支援中文、英文及中英並列。手機優先提供系統分享／儲存影像及長按存圖，桌面直接下載。圖片由原生 Canvas 在記憶體產生，不使用或複製大型截圖套件。
- 尚未包含：銅錢擲卦、數字起卦、時間起卦、精確到分鐘的節氣天文資料、月破日破、進退神、反吟伏吟、三合六合與斷卦文字。
- 股市頁仍透過公共 CORS proxy 抓 Yahoo Finance，可靠度受外部服務影響。

## 公開網站部署

- GitHub repository：`https://github.com/drtienk/ichingway_naja`
- GitHub Pages：`https://drtienk.github.io/ichingway_naja/`
- 推送到 `main` 後，由 `.github/workflows/pages.yml` 自動發布靜態檔案；不在 OneDrive 本機產生 build 或 dist。

## 驗收標準

- `node tests/najia-core.test.js` 全部通過。
- 根目錄 `index.html` 會進入 `najia/index.html`，裝卦是預設畫面，股市是第二個按鈕。
- 64 種上下卦組合都能得到唯一卦名與完整六爻。
- 384 種單爻變化都能得到變卦，且化爻六親以本卦宮五行計算。
- `易經股市應證.html` 的新入口能正確開啟 `najia/index.html`。
- `najia/index.html` 的返回入口能回到既有股市頁。
- 裝卦頁新增「萬年曆」「風水羅盤」兩個按鈕；股市頁也提供裝卦、萬年曆、風水羅盤入口，四個功能頁之間的導覽連結都必須正確。
- 萬年曆能切換月份、日期、國曆／農曆、字體及語言，並顯示所選日期的年/月/日干支與旬空。
- 羅盤能載入地圖，以座標下盤並切換正北／磁北、圖層、二十四山、顯示狀態及量向線；拒絕定位權限時仍可手動輸入地址或座標。
- iPhone 13 mini、iPhone 8、iPad mini 三種指定 viewport 均無頁面水平溢出。
- 中文預設模式在 iPhone 8 首屏可完整選擇上卦、下卦並按下排卦按鈕，不需向下捲動。
- 動爻圓圈／叉號與爻線重疊，世／應位於爻線下方；手機逐爻版的六親與納甲同列、六獸固定在右側。
- 有變卦時，手機畫面、桌面表格與存圖均為左變卦、右本卦；變卦只顯示動爻位置，非動爻位置不得畫出變卦爻線或文字。
- 中文、English、中英三個選項均可切換，重整頁面後保留選擇，且三種模式的頁面與匯出文字一致。
